# CLAUDE.md — Insurance Quoter Frontend

> Este archivo complementa las instrucciones globales en `~/.claude/CLAUDE.md` y las del monorepo en `../CLAUDE.md`.

## Ruta del proyecto

El working directory de este proyecto es:

```
/home/jorge-ruiz/Documentos/Sofka/Insurance-Quoter/Sofka-IQ/Insurance-Quoter-Front
```

Todas las rutas de archivos, comandos Bash, `find`, `grep` y referencias de código deben usar esta ruta como base. No usar rutas del monorepo padre (`Sofka-IQ/`) directamente.

Los documentos de referencia (api-contracts.md, prototipos JSX, etc.) están dentro de este mismo directorio bajo `docs/Sofka IQ/`.
